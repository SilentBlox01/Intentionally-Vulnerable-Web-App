run = "cp -n .env.example .env 2>/dev/null || true; cp -n config.js.example config.js 2>/dev/null || true; [ ! -d \"node_modules\" ] && npm install; npm start"

[nix]
channel = "stable-23_05"

[env]
XDG_CONFIG_HOME="/home/runner/$REPL_SLUG/.config"
