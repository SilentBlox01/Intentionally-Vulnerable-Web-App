version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - securetrust_data:/app/database/data
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=development
    restart: unless-stopped

volumes:
  securetrust_data:
